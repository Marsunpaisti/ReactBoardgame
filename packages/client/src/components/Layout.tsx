import { GameBoard } from './GameBoard';
import './Layout.scss';

function Layout() {
    return (
        <div className="contentContainer">
            <GameBoard />
        </div>
    );
}

export default Layout;
